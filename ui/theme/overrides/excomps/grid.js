export default (theme) => {
    return {
        MuiGrid: {
            root: { padding: theme.spacing(0) },
            container: { padding: theme.spacing(0) },
            item: { padding: theme.spacing(0) }
        }
    }
}